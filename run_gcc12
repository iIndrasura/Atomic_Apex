mkdir build
cd build

# Specify the C and C++ compilers using environment variables
CC=gcc-12 CXX=g++-12 cmake ..

# Use make to build the project with the specified compilers
make -j8

cd ..

# Run your program
sudo ./build/Atomic/Atomic
